import React, { FC } from 'react';
import './Header.scss';

export const Header: FC = () => (
  <header className="header">
    <div className="header__block" />
    <div className="header__block" />
  </header>
);
